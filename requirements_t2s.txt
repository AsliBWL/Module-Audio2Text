# requirements.txt
# Файл зависимостей для модуля синтеза речи (Text-to-Speech)
# Установка: pip install -r requirements_t2s.txt

# =====================================================
# Основные библиотеки
# =====================================================

# NumPy - работа с многомерными массивами и математическими операциями
# Используется для: хранения и обработки аудиоданных, преобразований
numpy>=1.24.0,<2.0.0
torch>=2.0.0


# =====================================================
# Синтез речи (TTS - Text-to-Speech)
# =====================================================

# qwen-tts - официальная библиотека для моделей Qwen3-TTS
# Используется для: загрузки и работы с моделями Qwen3-TTS
qwen-tts>=0.1.0

# transformers - библиотека Hugging Face для работы с трансформерами
# Используется для: загрузки моделей с Hugging Face Hub
# Необходима для Qwen3-TTS
transformers>=4.30.0

# huggingface-hub - клиент для взаимодействия с Hugging Face Hub
# Используется для: скачивания моделей и токенизаторов
huggingface-hub>=0.20.0


# =====================================================
# Конфигурация моделей
# =====================================================

# omegaconf - библиотека для управления конфигурационными файлами
# Используется для: загрузки конфигураций моделей Silero TTS
# Без этой библиотеки Silero TTS не работает!
omegaconf>=2.3.0


# =====================================================
# Захват и воспроизведение аудио
# =====================================================

# sounddevice - библиотека для захвата и воспроизведения звука
# Используется для: воспроизведения синтезированного аудио через динамики
sounddevice>=0.4.6

# soundfile - библиотека для чтения и записи аудиофайлов
# Используется для: сохранения синтезированного аудио в WAV-файлы
soundfile>=0.12.1


# =====================================================
# Обработка аудиосигналов
# =====================================================

# librosa - библиотека для анализа и обработки аудиосигналов
# Используется для: подготовки референсного аудио для клонирования
# Необходима для Qwen3-TTS
librosa>=0.10.0


# =====================================================
# Фреймворк машинного обучения
# =====================================================

# torch - основная библиотека машинного обучения
# Используется для: работы с моделями Silero TTS и Qwen3-TTS

# torchaudio - библиотека для работы с аудио от PyTorch
# Используется для: дополнительной обработки аудиосигналов
torchaudio>=2.0.0


# =====================================================
# Примечания по установке
# =====================================================

# Для работы с GPU NVIDIA (CUDA):
# PyTorch с поддержкой CUDA устанавливается отдельно:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118

# Для работы только на CPU (стандартная установка):
# torch и torchaudio установятся автоматически с CPU-версией

# Для Flash Attention (ускорение Qwen3-TTS на совместимых GPU):
# Требует NVIDIA GPU с архитектурой Ampere или новее (RTX 3090, A100, H100 и т.д.)
# Устанавливается отдельно, если нужна максимальная производительность:
# flash-attn>=2.0.0
# ВНИМАНИЕ: установка может занять длительное время (~5-10 минут)

# Для работы SoX (используется librosa при обработке аудио):
# SoX — это системная программа, она НЕ устанавливается через pip.
# Инструкция по установке для разных ОС:
#   - Windows:
#       1. Скачайте установщик с http://sox.sourceforge.net/
#       2. Запустите установку и запомните папку, куда установили SoX
#          (например, C:\Program Files (x86)\sox-14.4.2)
#       3. ДОБАВЬТЕ ПУТЬ К ПАПКЕ С sox.exe В ПЕРЕМЕННУЮ PATH:
#          - Нажмите Win+R, введите sysdm.cpl → Enter
#          - Перейдите на вкладку "Дополнительно" → "Переменные среды..."
#          - Найдите переменную Path, выберите "Изменить"
#          - Добавьте новую строку с путём к папке SoX (например, C:\Program Files (x86)\sox-14.4.2)
#          - Нажмите ОК во всех окнах и перезапустите терминал/среду разработки
#   - Linux: sudo apt-get install sox (путь добавится автоматически)
#   - macOS: brew install sox (путь добавится автоматически)


# =====================================================
# Web-сервер (FastAPI)
# =====================================================

# fastapi - современный веб-фреймворк для создания API
# Используется для: создания REST API для генерации аудио
fastapi>=0.104.0

# uvicorn - ASGI-сервер для запуска FastAPI приложений
# Используется для: запуска веб-сервера
uvicorn[standard]>=0.24.0

# pydantic - библиотека для валидации данных
# Используется для: валидации запросов/ответов API
pydantic>=2.0.0
